package com.hangir.data;

import java.util.Map.Entry;

public class HashMap {
	/*
	 * private final int DEFAULT_TABLE_SIZE = 2048;
	private float threshold = 0.75f;
	private int maxSize = 2048000;
	private int size = 0;
	
	Entry[] table;
	
	HashMap () {
		table = new Entry[DEFAULT_TABLE_SIZE];
		for (int i = 0; i < DEFAULT_TABLE_SIZE; i++) {
			table[i] = null;
		}
	}
	void setThreshold(float threshold) {
			this.threshold = threshold;
			maxSize = (int) (table.length*threshold);
		}
	
	
	public int get(int key) {
		int hash = (key % table.length);
		int initialHash = -1;
		while (hash != initialHash && (table[hash] == DeletedEntry.getUniqueEntry()
				|| table[hash] != null
				&& table[hash].getKey() != key)) {
			
		}
				
	}
	*/
	}
	

